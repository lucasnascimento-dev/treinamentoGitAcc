<mvc:View
    controllerName="sap.ui5.odata.projectodata3.controller.Object"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:semantic="sap.f.semantic">

    <semantic:SemanticPage
        id="page"
        headerPinnable="false"
        toggleHeaderOnTitleClick="false"
        busy="{objectView>/busy}"
        busyIndicatorDelay="{objectView>/delay}"
        showFooter="true">

        <semantic:titleHeading>
            <Title
                text="Details"
                level="H2"/>
        </semantic:titleHeading>

        <semantic:headerContent>
        </semantic:headerContent>

        <semantic:content>
            <Panel >
            <VBox >
                <Label text="Id" />
				<Input id="inId" value="{ID}" width="200px"/> 

                <Label text="Name" />
				<Input id="inName" value="{Name}" width="200px"/> 
            </VBox>
            </Panel>
        </semantic:content>

        <semantic:sendEmailAction>
            <semantic:SendEmailAction id="shareEmail" press=".onShareEmailPress"/>
        </semantic:sendEmailAction>

        <semantic:footerCustomActions>
            <Button text="Create" press="onCreate" id="btnCreate" />
            <Button text="Edit" press="onEdit" id="btnEdit" />
            <Button text="Delete" press="onDelete" id="btnDelete" />
            <Button text="Save" press="onSave" id="btnSave" />
        </semantic:footerCustomActions>

    </semantic:SemanticPage>

</mvc:View>